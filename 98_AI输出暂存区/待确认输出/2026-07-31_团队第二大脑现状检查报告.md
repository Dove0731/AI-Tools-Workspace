# 团队第二大脑现状检查报告

> 输出编号：AIOUT-202607-003  
> 检查日期：2026-07-31  
> 当前状态：待确认  
> 检查范围：`E:\新AI工具人`  
> 说明：本报告只记录路径、类型、数量和风险分类，未记录任何密码、令牌或密钥内容。

## 一、结论

本目录已具备 Obsidian 工作库和 Codex 根目录规则，但尚未形成有效 Git 仓库，也没有 GitHub 登录工具、远程仓库或提交身份。当前目录包含大量可重新生成依赖以及加密凭据和内部业务原始资料，必须先应用忽略规则、核对拟上传范围，再创建 Private 仓库和首次推送。

## 二、已满足

| 项目 | 检查结果 | 证据 |
|---|---|---|
| 工作目录 | 有效 | `E:\新AI工具人` 可读写 |
| Obsidian vault | 已存在 | 根目录存在 `.obsidian` |
| Codex 规则 | 已存在 | 根目录存在 `AGENTS.md` |
| 工作库说明 | 已存在 | 根目录存在 `README.md` |
| Git 可执行程序 | 可用 | Codex 工作区提供 Git 2.53.0.windows.3 |
| 超过 50 MB 的单文件 | 本轮未发现 | 全库文件大小统计 |

## 三、需要自动处理

| 项目 | 检查结果 | 处理动作 |
|---|---|---|
| 根目录 `.gitignore` | 原检查时不存在 | 创建并验证 |
| Git 仓库 | 存在空 `.git` 目录，但不是有效仓库 | 使用 `git init -b main` 正常初始化 |
| 协作规范 | 缺少专项规则 | 创建架构、协作规范、贡献指南和 PR 模板 |
| 权限台账 | 缺少 | 创建 GitHub 协作与权限台账 |
| 临时依赖 | 数量和体积过大 | 忽略 `node_modules`、`.codex_tmp`、`.tmp_k3` 和缓存 |

## 四、需要用户确认

| 事项 | 当前状态 | 所需确认 |
|---|---|---|
| Git 提交用户名 | 未配置 | GitHub 用户名或希望显示的提交名称 |
| Git 提交邮箱 | 未配置 | GitHub 注册邮箱或 noreply 邮箱 |
| GitHub 仓库所有者 | 未确认 | 个人账号或公司组织 |
| 仓库名称 | 建议 `cosmetics-second-brain` | 是否采用建议名称 |
| 成员及权限 | 未提供 | GitHub 用户名、角色和最小权限 |
| 受控业务资料 | 未授权上传 | 是否允许纳入首次 Private 仓库 |
| 备份机制 | 未建立 | 责任人、频率、保存位置和恢复演练 |

## 五、禁止上传

| 路径/规则 | 风险分类 | 处理 |
|---|---|---|
| `07_AI工作区/金蝶MCP连接/credential.clixml` | 加密凭据 | 由 `.gitignore` 排除，禁止进入 Git |
| `.env`、`.env.*` | 环境凭据候选 | 排除 |
| `*.key`、`*.pem`、`*.pfx`、`*.p12`、`*.clixml` | 密钥或凭据候选 | 排除 |
| 任意 `node_modules` | 可重新生成依赖 | 排除 |
| `.codex_tmp`、`.tmp_k3`、`tmp` | 临时处理和解压目录 | 排除 |

`07_AI工作区/金蝶MCP连接/save_credential.ps1` 因文件名包含 credential 被扫描命中，但它是凭据保存脚本而非凭据本体。是否允许进入 Private 仓库需通过内容检查后决定；不得因为文件名判断而删除。

## 六、建议仅在 Private 仓库受控保存

| 分类 | 已发现路径示例 | 主要风险 |
|---|---|---|
| 配方与研发 | `02_会议与协同/研发配方对接`、`09_产品开发管理/配方开发协同规则.md` | 配方和研发协同机密 |
| 采购底价与核价 | `05_问题反馈与复盘/03_成本异常`、`99_归档/原始资料归档/OEM核价` | 采购底价、报价体系和毛利风险 |
| 付款和现金流 | `02_会议与协同/财务付款对接`、`10_采购与成本管理/付款与现金流` | 付款、账期和现金流信息 |
| 原始订单与备案资料 | `99_归档/原始资料归档` | 原始业务数据、客户/供应商和附件风险 |
| AI 暂存输出 | `98_AI输出暂存区` | 尚未人工确认，不能视为正式事实 |

## 七、规模和存储风险

初始全量扫描约 65,072 个文件、3.24 GB。主要体积来自：

| 顶层目录 | 文件数 | 约占空间 | 判断 |
|---|---:|---:|---|
| `outputs` | 33,325 | 1.57 GB | 含多份 `node_modules` 和生成结果，需按忽略规则重算 |
| `.codex_tmp` | 15,576 | 0.73 GB | 临时处理目录，禁止纳入 Git |
| `.tmp_k3` | 15,545 | 0.73 GB | 临时处理目录，禁止纳入 Git |
| `99_归档` | 76 | 187 MB | 受控上传候选，含原始业务资料 |

重复文件组主要来自多份 `node_modules`，不建议为其建立 Git 历史。应用忽略规则后需重新统计拟纳入文件和体积。

### 应用忽略规则后的拟纳入范围

重新统计结果为 2,860 个文件、约 327.4 MB。其中：

| 顶层目录 | 文件数 | 约占空间 | 首次上传建议 |
|---|---:|---:|---|
| `99_归档` | 76 | 187.3 MB | 含原始业务资料，必须由用户确认 |
| `outputs` | 2,237 | 113.2 MB | 主要为生成结果和图片裁切，建议确认是否需要进入首版 |
| `10_采购与成本管理` | 30 | 10.7 MB | 采购和成本敏感资料，限 Private 受控保存 |
| `11_张总分享专区` | 32 | 8.6 MB | 内部经营资料，限 Private 受控保存 |
| 其他目录 | 485 | 约 7.6 MB | 仍需首次 Push 前复核 |

拟纳入文件包括约 407 个 Markdown、100 个 Excel、24 个 PDF、14 个 PowerPoint、9 个 Word 和 1,141 个 JPG/PNG。最大未忽略单文件约 33.9 MB，路径为：

`99_归档/原始资料归档/采购订单/2026/2026-07-13_采购在途订单原件/采购在途25.10.26新版 (1).xlsx`

当前没有单文件超过 50 MB，普通 Git 可以技术性纳入；但二进制资料约占绝大多数空间，后续历史增长会显著放大仓库。Git LFS 或缩小首版上传范围均值得评估，本轮未安装 Git LFS。

### 未忽略文本的高置信度密钥扫描

扫描命中 `07_AI工作区/金蝶MCP连接/kingdee_mcp_server.py` 第 151 行的 `password` 字段。结构检查显示该行由变量/属性组成，没有字符串常量，因此当前判断为代码变量命中而非已发现的硬编码密码；实际值未输出。首次 Push 前仍需再次复核。

## 八、工具与远程状态

| 项目 | 状态 |
|---|---|
| GitHub CLI | 未发现 |
| GitHub Desktop | 未发现 |
| GitHub 连接器 | 当前未提供 |
| GitHub 登录 | 未验证 |
| origin | 不存在 |
| GitHub 仓库 | 未创建 |
| 首次推送 | 未执行 |
| Git 本地仓库 | 已初始化，分支为 `main`，尚无 Commit |

## 九、下一步

1. 建立并验证 `.gitignore`。
2. 建立协作规则、权限台账和 Pull Request 模板。
3. 初始化本地 Git `main` 分支。
4. 重新统计拟纳入文件、体积、敏感路径和忽略结果。
5. 用户提供 Git 提交用户名和邮箱后完成首次本地提交。
6. 用户确认仓库所有者、名称和上传范围后，再建立 Private GitHub 仓库。
7. 第一次 Push 前再次展示拟上传清单，并等待明确确认。
