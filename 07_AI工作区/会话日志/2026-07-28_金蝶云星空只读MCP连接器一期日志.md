# 2026-07-28 金蝶云星空只读 MCP 连接器一期日志

## 任务编号

`AI-202607-007`

## 目标

先建立 Codex 与金蝶云星空企业版私有云之间的只读 MCP 连接，不新增、不审核、
不反审核、不删除、不付款。

## 用户确认口径

- 接口方式：WebAPI；
- 认证方式：用户认证；
- 接口账号：曾德炜；
- 登录语言参数：`lcid`；
- 数据中心：使用现有数据中心；
- 第一期开放：连接测试、采购订单、收料/入库、即时库存、供应商、物料查询；
- 暂不开放：新增采购订单；
- 禁止：审核、反审核、删除、付款。

## 已完成

1. 新建无第三方依赖的 Python MCP 服务；
2. 仅注册 6 个核准工具，代码中不实现 5 个禁止/暂不开放工具；
3. 查询统一走金蝶 `ExecuteBillQuery`，单次上限 200 行；
4. 新建项目级 `.codex/config.toml`，同时配置允许与禁止工具清单；
5. 新建 PowerShell 配置脚本，密码使用 Windows 当前用户加密的
   `credential.clixml` 保存，不写入普通 JSON；
6. 新建使用说明、配置样例、忽略规则和自动测试；
7. 5 项测试通过，验证工具清单、禁止工具拒绝、文档地址拒绝、
   MCP 初始化及 stdio 启动。

## 当前状态

2026-07-28 20:05，正式账套 WebAPI 登录验证成功：

- 服务地址：`http://39.98.44.53/k3cloud/`；
- 数据中心：`2023七遇集团`；
- 数据中心 ID：`64464dd5f498e6`；
- 账号：曾德炜；
- 登录结果：`LoginResultType = 1`；
- 业务单据读取：否；
- 写入操作：否。

状态更新为“连接已验收、业务查询待验收”。测试结果见
`07_AI工作区/金蝶MCP连接/connection_test_20260728.json`。

## 待确认

1. 后续用 1—3 张采购订单校验企业定制字段；
2. 分别验收采购入库、库存、供应商和物料查询字段；
3. 核验只读账号在所有组织下的实际数据可见范围。

## 验收门槛

- `test_connection` 明确返回登录成功；
- 金蝶侧没有新增、修改、审核、反审核、删除或付款记录；
- 首次业务查询字段与人工核对结果一致后，才能将业务查询状态升级为“已验收”。
