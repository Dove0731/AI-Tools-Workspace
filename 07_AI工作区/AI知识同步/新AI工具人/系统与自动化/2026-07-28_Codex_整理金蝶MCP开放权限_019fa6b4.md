---
project: "新AI工具人"
source: Codex
source_id: 019fa6b4-f584-7450-ac12-349e2a3ad3e5
category: 系统与自动化
updated: 2026-07-28
sync_status: synced
tags: [AI知识同步]
---

# 整理金蝶MCP开放权限

> 自动提取的核心会话正文；涉及业务数据时仍以正式台账和原始凭证为准。

## 用户需求

| MCP工具 | 用途 | 第一期 |
|---|---|---|
| `test_connection` | 测试金蝶连接 | 开放 |
| `query_purchase_orders` | 查询采购订单 | 开放 |
| `query_purchase_receipts` | 查询收料/入库 | 开放 |
| `query_inventory` | 查询即时库存 | 开放 |
| `query_suppliers` | 查询供应商资料 | 开放 |
| `query_materials` | 查询物料资料 | 开放 |
| `create_purchase_order` | 新增采购订单 | 暂不开放 |
| `audit_document` | 审核单据 | 禁止 |
| `unaudit_document` | 反审核 | 禁止 |
| `delete_document` | 删除单据 | 禁止 |
| `payment_operation` | 付款操作 | 禁止 |

## AI输出

已确认金蝶 MCP 第一期权限边界：

- 开放：连接测试，以及采购订单、收料/入库、即时库存、供应商、物料资料的只读查询。
- 暂不开放：新增采购订单。
- 严格禁止：审核、反审核、删除单据和付款操作。

后续设计与调用将遵循“只读优先、最小权限”原则；禁止项不提供调用入口，查询结果不自动触发任何金蝶数据变更。

## 用户需求

| 产品版本 | 金蝶云星空企业版
| 部署方式 | 私有云
| 访问方式 | HTML5网址或服务地址 |
| 测试账套 | 测试数据中心/账套ID |
| 接口方式 | WebAPI
| 认证方式 | 用户认证
| 接口账号 | 曾德炜
| 组织范围 | 所有组织 
| 网络要求 | 无限制
| 接口文档 | https://openapi.open.kingdee.com/ApiDoc
| 测试单据 | 1—3张可人工核对的采购订单 |

## AI输出

已记录金蝶接口环境：

- 产品：金蝶云星空企业版，私有云部署
- 接口：WebAPI，用户认证
- 接口账号：曾德炜
- 组织范围：所有组织
- 测试范围：1—3 张可人工核对的采购订单
- 官方文档：[金蝶开放平台接口文档](https://openapi.open.kingdee.com/ApiDoc)

开始联调前仍需补齐：

1. WebAPI 服务地址（建议精确到服务器根地址）
2. 测试数据中心/账套 ID
3. 接口账号密码或安全凭证
4. 登录语言参数（通常为 `lcid`）
5. 用于核验的采购订单编号
6. 各组织的组织编码及名称

安全建议：即使网络目前“不限制”、组织范围为“所有组织”，第一期接口仍应强制只读，并限制测试环境、来源 IP、账号权限和日志脱敏；密码不要写入代码、配置样例或工作库正文。

## 用户需求

1.https://openapi.open.kingdee.com/ApiDoc
2.使用现有数据中心，无需使用测试中心
3.账号密码或安全凭证，手动输入即可
4.登录语言参数为lcid
5.核验的订单暂时不着急，先链接好两个系统

## AI输出

我先按“只建立连接、不查询业务单据、不写入金蝶”推进。我会检查现有工作库里的系统入口与 MCP 配置，并使用 OpenAI 官方文档技能核对 Codex 侧的 MCP 接入方式；同时需要指出：你给的地址目前是接口文档地址，不一定是你们私有云金蝶的 WebAPI 服务地址。

## AI输出

现阶段的关键阻塞已经明确：`https://openapi.open.kingdee.com/ApiDoc` 只能用于看文档，不能直接登录你们私有云账套。我要先把 Codex 侧的本地 MCP 服务和安全配置骨架搭好；真正完成“连通”还必须由你手动提供私有云 WebAPI 根地址、数据中心 ID 和密码。账户密码不会落盘。

## AI输出

我会把第一期做成一个独立、只读的金蝶 MCP 连接器：只暴露 6 个允许工具，代码层面根本不实现新增、审核、反审核、删除和付款。凭据采用 Windows 当前用户加密保存，配置文件不写明文密码；在缺少私有云地址和数据中心 ID 时，连接器可以安装并自检，但不会虚报“已连通”。现在我在核对金蝶 WebAPI 的登录与查询端点。

## AI输出

连接器与项目级 MCP 配置已经写入，权限边界已做成“双保险”：服务端只注册 6 个只读工具，Codex 配置又设置了允许清单和禁止清单。接下来我正在跑协议、自检和配置解析测试；因为缺少真实私有云地址和数据中心 ID，真实登录测试仍会明确保持“待配置”，不会被算作已连通。

## AI输出

自检发现两处 Windows 编码兼容问题：旧版 PowerShell 会把无 BOM 的中文脚本误读，Python 子进程测试也默认按 GBK 解码。我正在把启动脚本改为纯 ASCII（中文说明保留在 README），并固定测试输出为 UTF-8；这不影响业务逻辑，但会影响 MCP 能否稳定启动。

## AI输出

金蝶只读 MCP 一期技术连接器已完成，但真实系统登录仍为“待确认”，不能宣称已经连通。

已完成：

- 仅开放 6 个只读工具，未实现新增、审核、反审核、删除和付款接口。
- 接入金蝶登录验证及 `ExecuteBillQuery` 查询端点，符合金蝶 WebAPI 文档口径。[金蝶接口说明](https://vip.kingdee.com/article/490771221039228672)
- 密码使用 Windows 当前用户加密保存，不写入普通配置文件。
- 项目级 MCP 配置已建立，Codex 官方支持通过 `.codex/config.toml` 配置本地 stdio MCP 服务。[Codex 配置参考](https://learn.chatgpt.com/docs/config-file/config-reference#configtoml)
- 5 项自动测试全部通过，包括权限清单、禁止工具拒绝、MCP 启动和 UTF-8 验证。
- 已同步 AI 工作区索引、任务台账 `AI-202607-007` 和会话日志。

关键文件：

- [使用说明](E:\新AI工具人\07_AI工作区\金蝶MCP连接\README.md)
- [首次配置脚本](E:\新AI工具人\07_AI工作区\金蝶MCP连接\setup_connection.ps1)
- [MCP 服务代码](E:\新AI工具人\07_AI工作区\金蝶MCP连接\kingdee_mcp_server.py)
- [Codex MCP 配置](E:\新AI工具人\.codex\config.toml)

下一步请运行：

```powershell
& 'E:\新AI工具人\07_AI工作区\金蝶MCP连接\setup_connection.ps1'
```

需要手动输入：

1. 你们企业私有云金蝶的实际 HTML5/服务根地址；
2. 现有数据中心/账套 ID；
3. 密码。

`https://openapi.open.kingdee.com/ApiDoc` 是文档站，不能用于企业账套登录。配置完成后重新打开本工作区任务，再调用 `test_connection`；只有登录验证成功，才能确认两个系统已经连通。

<oai-mem-citation>
<citation_entries>
MEMORY.md:665-677|note=[kept technical completion separate from real connection acceptance]
</citation_entries>
<rollout_ids>
019f823b-7f09-7121-ba7e-a96a461ed203
</rollout_ids>
</oai-mem-citation>

## 用户需求

我看不懂你说的方式，你可以直接接管电脑继续操作

## AI输出

可以。我会直接操作电脑，先从你当前已登录的金蝶页面找出私有云地址和数据中心信息，再打开配置窗口。遇到密码输入时我会停在密码框，让你本人输入；密码不会出现在聊天或普通配置文件中。现在使用电脑控制技能继续。

## AI输出

我找到了已打开的 Chrome 窗口，标题是“采购在途25.10.26新版”，很可能就是金蝶页面。电脑控制技能要求浏览器内操作改用 Chrome 控制通道，我现在切换过去读取当前页面地址和账套信息，不会修改任何单据。

## AI输出

Chrome 的专用控制连接暂时不可用，但电脑控制仍能看到这一个已打开的 Chrome 窗口。我会只读取当前金蝶页面的地址和可见账套信息；不会操作登录框、不会查看保存的密码，也不会修改页面数据。

## AI输出

电脑接管被系统安全机制自动停止：当前无法可靠识别 Chrome 页面网址，因此不允许继续操作浏览器。

请先在 Chrome 中打开金蝶登录页或已登录的金蝶页面，然后把地址栏里的网址复制发给我。收到网址后，我再继续接管并完成配置；密码环节会留给你本人输入。

## 用户需求

http://39.98.44.53/k3cloud/

## AI输出

收到，这就是私有云根地址：`http://39.98.44.53/k3cloud/`。我现在继续接管，只读取登录页中的数据中心信息并完成本地只读连接配置；密码仍由你本人输入。

## AI输出

地址已确认，但电脑控制再次被安全机制停止：它仍无法从 Chrome 窗口独立确认当前网址，因此本轮不能继续点击。

下一步我将改用 Codex 内置浏览器打开该地址，读取数据中心信息并继续配置，不再依赖 Chrome 接管。

## 用户需求

继续

## AI输出

我改用 Codex 内置浏览器继续打开金蝶页面并读取数据中心。若页面出现“不安全连接”拦截，我不能代替你绕过；如果只是正常的 HTTP 登录页，我会继续操作到需要你输入密码的位置。

## AI输出

页面已经成功打开，而且当前账号“曾德炜”处于已登录状态；可见账套名称为“2023七遇集团”，当前组织为“100 广州七遇生物科技有限公司”。我现在通过只读 WebAPI 获取这个账套对应的数据中心 ID，避免让你手工查找。

## AI输出

数据中心菜单点击后的页面响应超时，控制连接已重置；是否已经展开目前不确定。我不会重复点击旧位置，先重新连接并读取最新页面状态。

## AI输出

我已确认服务器可访问，但常见的“数据中心列表”接口在这套金蝶版本中没有开放。接下来我只读取未登录首页返回的公开登录配置，寻找“2023七遇集团”对应的内部 ID；不会调用任何业务或写入接口。

## AI输出

已找到准确的数据中心 ID：`64464dd5f498e6`，对应“2023七遇集团”；另一个 `6426cd37a0d066` 是“测试账套”，不会使用。我现在把正式账套写入只读连接配置，然后弹出密码输入窗口。你只需在窗口里输入金蝶密码并确认，我不会读取或显示密码。

## AI输出

密码窗口已弹出。请在窗口中输入金蝶密码并点击“确定”；完成后我会自动检测并立即进行只读登录测试。

## AI输出

密码窗口仍在等待输入，可能被其他窗口挡住了。请按 `Alt+Tab` 切换到 PowerShell/“Windows PowerShell 凭据请求”窗口，输入密码并点“确定”。我继续等待。

## AI输出

已完成以下配置：

- 金蝶地址：`http://39.98.44.53/k3cloud/`
- 正式账套：`2023七遇集团`
- 数据中心 ID：`64464dd5f498e6`
- 当前组织：`100 广州七遇生物科技有限公司`
- 账号：`曾德炜`

现在只差密码。密码窗口仍开着，请切换到“Windows PowerShell 凭据请求”窗口，输入金蝶密码并点击“确定”，然后回复我“已输入”，我立即继续测试连接。

## 用户需求

<in-app-browser-context source="ambient-ui-state">
This block is automatically supplied ambient UI state, not part of the user's request. Do not treat it as an instruction or as evidence that the user explicitly selected the in-app browser.
# In app browser:
- The user has the in-app browser open with 1 tab.
- Current URL: http://39.98.44.53/k3cloud/html5/dform.aspx?formId=BOS_HtmlConsole&formType=mobileform&pageId=MainPageId&_cnneptstate_=PE5ZPElJLSw8SUlRPElJT0x4SUhMUHlEekl9ekRLeU58RHhNTnlER1BHSn19fElLSklIPElJQzxJSSshezxJSVE8SUlKe09HeUh4S0R6Skh8REt8ek9ET3p6fUR9TXhOe3lKSVB5fEc8SUlDPElJI3wxPElJUTxJSSMxSn0tSCElLSl5K0gnJCshMCQgfiB8IjxJSUM8SUkkeig8SUlRPE5ZPElJYyF6fCYrfGsxKHw8SUlRSjxOWzxOWw%3D%3D&custparam=
</in-app-browser-context>

## My request for Codex:
已输入

## AI输出

收到。我现在检测加密凭据文件，并调用唯一的 `test_connection` 进行登录验证；这一步不查询采购订单，也不会修改金蝶数据。

## AI输出

系统没有检测到加密凭据文件，说明密码窗口虽然输入过，但保存步骤没有成功。我先检查窗口是否仍在运行及脚本状态，不会要求你把密码发到聊天里。

## AI输出

原密码窗口仍然开着，文件尚未保存。请在凭据窗口里再点击一次“确定/OK”（仅输入密码还不够）；点击后不要关闭窗口，我会自动检测。

## AI输出

当前弹出的其实是 PowerShell 控制台提示，不够直观。我会关闭这个由我启动且尚未保存任何凭据的旧窗口，换成一个清晰的“密码输入框 + 保存按钮”窗口；你只需输入密码并点击“保存”。这次不需要操作命令行。

## AI输出

新的密码窗口已经居中弹出，标题是 `Kingdee MCP Credential`。请在 `Password` 输入框填写金蝶密码，然后点击 `Save`。保存后窗口会自动关闭，我继续检测。

## AI输出

凭据已成功加密保存。现在执行只读登录验证。

## AI输出

登录测试已到达金蝶服务器，但发现连接器把你提供的 `/k3cloud/` 路径又重复拼接了一次，服务器因此拒绝；同时 PowerShell 管道首行带了 BOM。账号密码尚未被判定错误。我现在修正这两个连接层问题后重新测试。

## AI输出

连接已经成功：金蝶返回 `LoginResultType = 1`，账号“曾德炜”通过 WebAPI 登录验证；本次未读取任何采购单据，也没有发生写入。接下来我会把状态同步为“连接已验收、业务查询待验收”，并保存一份不含密码的测试结果。

## AI输出

连接已完成并通过验证。

- 正式账套：2023七遇集团
- 数据中心 ID：`64464dd5f498e6`
- 账号：曾德炜
- 验证结果：`LoginResultType = 1`
- 状态：连接已验收
- 本次未读取业务单据、未执行任何写入操作
- 6 项自动测试全部通过
- 密码已由 Windows 当前用户加密保存

测试记录：[connection_test_20260728.json](E:\新AI工具人\07_AI工作区\金蝶MCP连接\connection_test_20260728.json)

下一阶段是用 1—3 张采购订单核验查询字段。以后你可以直接说：“查询金蝶采购订单”，无需再重复配置。

## 用户需求

# Files mentioned by the user:

## 正彩测试版面膜（测试）.xlsx: C:/Users/Administrator/Desktop/正彩测试版面膜（测试）.xlsx

<in-app-browser-context source="ambient-ui-state">
This block is automatically supplied ambient UI state, not part of the user's request. Do not treat it as an instruction or as evidence that the user explicitly selected the in-app browser.
# In app browser:
- The user has the in-app browser open with 1 tab.
- Current URL: http://39.98.44.53/k3cloud/html5/dform.aspx?formId=BOS_HtmlConsole&formType=mobileform&pageId=MainPageId&_cnneptstate_=PE5ZPElJLSw8SUlRPElJT0x4SUhMUHlEekl9ekRLeU58RHhNTnlER1BHSn19fElLSklIPElJQzxJSSshezxJSVE8SUlKe09HeUh4S0R6Skh8REt8ek9ET3p6fUR9TXhOe3lKSVB5fEc8SUlDPElJI3wxPElJUTxJSSMxSn0tSCElLSl5K0gnJCshMCQgfiB8IjxJSUM8SUkkeig8SUlRPE5ZPElJYyF6fCYrfGsxKHw8SUlRSjxOWzxOWw%3D%3D&custparam=
</in-app-browser-context>

## My request for Codex:
按表格格式尝试新建系统编码

## AI输出

我会先读取这份 Excel，确认要新建的是哪些编码、数量和必填字段，再与金蝶现有物料做只读查重。这里会使用电子表格技能解析文件；如果进入正式新建，我会按“先建 1 条、保存前核对、成功后再继续”的方式控制风险，不做审核、删除或其他业务单据操作。

## AI输出

Excel 中初步识别出 4 个目标：1 个成品“正彩测试版面膜”和 3 个待编码包材（膜袋、膜布、彩盒）；通用箱已有编码 `TY-NT-0010`，配方已有编码 `BM-P804`，不应重复新建。成品及 3 个包材的编码栏都是空白，我正在结合此前这项测试的拟用编码记录并先做金蝶查重。

## AI输出

电子表格内容已成功读取，但其内置渲染器在这份文件上发生本机崩溃；源文件没有被修改。我会用本机 Excel 只读导出预览完成视觉核验，这只是渲染兜底，不会保存或覆盖原表。随后按已记录的拟用编码查重：`ZC-M-H-0092` 及 3 个包材编码。

## AI输出

查重发现关键冲突：`ZC-M-H-0092` 已经是“正彩光感美白祛斑面膜28ml*5（独家款）”，并已在组织 100/101/102/105 审核启用，不能用于本表的“正彩测试版面膜25ml*5”。因此我不会按旧拟用编码新建，正在查询同系列最新编码，重新选取不冲突的测试编码。

## AI输出

我继续做“编码查重 + 首条试建”。现在先确认 `0095` 这一组编码和“正彩测试版面膜”名称在所有组织里都没有重复；确认无冲突后，只先试建成品主档，不触碰审核、删除或付款类操作。

## AI输出

查重结果明确：`ZC-M-H-0095` 及三个后缀目前均未占用，“正彩测试版面膜”也没有同名物料；配方 `BM-P804` 和通用内托 `TY-NT-0010` 已存在且已审核。下一步我会在金蝶中只试建 `ZC-M-H-0095` 成品物料，先保存为未审核状态，再反查验证。

## AI输出

我在物料列表里发现了一条已经存在的“创建”状态记录：名称是“正彩测试版面膜25ml*5”，编码为空、单位为盒。它很可能就是您刚才手动录入但尚未保存完整的记录，因此我不会再新增重复行，改为打开这条记录补充候选编码 `ZC-M-H-0095`。

## AI输出

表单内目前“物料属性”是“自制”，但源表明确写的是“委外”。我会同时纠正这一项，并补充规格 `25ml*5`；这样试建记录才与表格口径一致。其余未确认字段不擅自补写。

## AI输出

关键字段已复核：编码 `ZC-M-H-0095`、名称“正彩测试版面膜25ml*5”、规格 `25ml*5`、单位盒、尺寸单位厘米。系统未接受“物料属性”和“成品类别”的下拉改动，所以我先按现有表单保存为未审核试建记录，并会把这两项列为待确认，不提交、不审核。

## AI输出

保存动作后，金蝶把编码字段自动清空，且没有返回成功提示；这说明记录尚未真正建成。我正在用只读接口反查，确认系统是否实际生成了物料，避免因界面显示异常而重复创建。

## AI输出

浏览器自动化接口在这个金蝶旧版下拉控件上没有稳定触发保存反馈。我改用屏幕级控制直接操作当前金蝶页面，这种方式更适合它的老式表单控件；仍只处理这条未审核物料，不会提交或审核。

## AI输出

屏幕级控制不能用于应用内浏览器窗口，安全限制要求我停用该方式。当前最可靠的路线是调用金蝶官方 WebAPI 的“保存物料”接口，但这属于写入能力；我会先按官方字段格式校验请求，只尝试这一条未审核物料，并在写入后立即只读反查。

## AI输出

反查到真实原因了：系统里已经生成了物料 ID `1162826`，名称正确，但编码被保存成了空格，状态仍为“创建”。因此不再新建；我会只更新这条现有记录的“编码”和“规格”，避免覆盖您已经录入的其他字段。

## AI输出

WebAPI 已准确定位到空编码记录，但金蝶提示这条物料仍被当前“物料-修改”页面锁定。我先退出编辑页释放锁，再只更新编码与规格；不会删除这条记录。

## AI输出

首条系统编码已经写入成功：`ZC-M-H-0095`，对应物料 ID `1162826`。我正在做最后反查，确认名称、规格、组织和数据状态，并核对没有触发提交或审核。

## AI输出

已成功试建首条系统编码：

- 编码：`ZC-M-H-0095`
- 物料 ID：`1162826`
- 名称：正彩测试版面膜25ml*5
- 规格：`25ml*5`
- 单位：盒
- 组织：`100`
- 状态：创建、未禁用
- 未执行提交和审核

原计划编码 `ZC-M-H-0092` 已被其他产品占用，因此改用查重无冲突的 `0095`。本次是修复已有同名空编码记录，没有重复新增。

仍需确认：

- 表格写“委外”，系统当前为“自制”。
- 成品类别尚未填写。
- 膜布单位“片/个”存在冲突。
- `TY-NT-0010` 系统中是内托，不是外箱。
- 三个包材及BOM尚未建立。

已同步：

- [操作日志](</E:/新AI工具人/07_AI工作区/会话日志/2026-07-28_正彩测试版面膜金蝶编码试建日志.md>)
- [项目复盘](</E:/新AI工具人/05_问题反馈与复盘/05_项目复盘/RP-202607-002_金蝶云星空物料及BOM建档操作效率_复盘.md>)
- [问题闭环台账](</E:/新AI工具人/04_数据台账/问题闭环台账/问题闭环总台账.md>)

源表核对范围：:codex-file-citation{path="C:/Users/Administrator/Desktop/正彩测试版面膜（测试）.xlsx" artifact_kind="workbook" sheet="百媚生产下单表模版" range="A5:I37"}

<oai-mem-citation>
<citation_entries>
MEMORY.md:665-677|note=[used pilot versus formal closure status discipline]
</citation_entries>
<rollout_ids>
019f823b-7f09-7121-ba7e-a96a461ed203
</rollout_ids>
</oai-mem-citation>
