# 数据标准

本目录是业务台账及AI分析字段的正式中文标准入口。根目录 `data_schema` 为重构前保留的历史字段参考；如两处有差异，以本目录已确认的最新标准为准。

## 使用原则

1. AI分析前检查必填字段。
2. 不得补造缺失数据，缺失项标注“待确认”。
3. 金额注明币种、含税口径、税率及是否含运费。
4. 日期统一使用 `YYYY-MM-DD`。
5. 比价保证物料、规格、数量、税费、物流和付款条件口径一致。
6. 调整标准时同步对应台账、分析逻辑和索引。
