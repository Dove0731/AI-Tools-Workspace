# 团队第二大脑协作指南

## 开始工作

1. 使用 GitHub Desktop 或 Git 切换到 `main`。
2. Fetch/Pull 最新版本。
3. 确认本地没有未提交修改。
4. 为当前任务新建分支。
5. 使用 Obsidian 打开仓库根目录；需要 AI 处理时，让 Codex 在同一目录工作。

## 完成工作

1. 在 Obsidian 中检查内容、来源、链接和“待确认”标记。
2. 检查 Git Diff，确认没有凭据、隐私和无关改动。
3. 同步相应索引和台账。
4. Commit 并 Push 当前分支。
5. 创建 Pull Request，等待审核后合并。
6. 合并后返回 `main` 并再次 Pull。

## 重要限制

- 仓库必须保持 Private。
- 禁止提交 `.env`、密码、令牌、密钥和凭据文件。
- 未确认 AI 输出不得直接进入正式资料。
- 多人同时修改同一文件可能产生冲突，应提前划分文件责任人。
- 详细规则见 `00_工作台与规则/GitHub协作规范.md`。

